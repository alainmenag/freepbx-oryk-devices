<!-- /var/www/html/admin/modules/oryk_connect/module.xml -->
<module>
	<rawname>oryk_connect</rawname>
	<name>Connect</name>
	<repo>commercial</repo>
	<version>1.3.2</version>
	<dbversion>1.3.2</dbversion>
	<publisher>Oryk</publisher>
	<type>setup</type>
	<class>Oryk_connect</class>
	<phpclass>Oryk_connect</phpclass>
	<canuninstall>no</canuninstall>
	<candisable>no</candisable>
	<license>AGPLv3</license>
	<licenselink>http://www.gnu.org/licenses/agpl-3.0.txt</licenselink>
	<changelog>
		*1.3.2* PJSIP devices carry custom endpoint settings, starting with a from domain, written to pjsip.endpoint_custom_post.conf without disturbing what other modules keep in the same file; the domain is taken from the device's own From Domain field, or the From Domain in Settings -> Advanced Settings, or the PBX hostname when that is a domain name; a device gives its settings up when it is deleted and takes them with it when it is renumbered
		*1.3.1* Fixes deleting a device with call history, which failed in 1.3.0
		*1.3.0* Internal reorganisation only, with no change to what the module does: the one class the module was written in is now ten, each owning one thing, and saving a device no longer reads the web request directly. Any code calling this module's methods directly should be checked against the new names
		*1.2.2* The device list sorts only by the columns it offers and no longer names the asterisk database on its own query, so it works on an install that renamed it; the mailbox, User Manager and call history handling has moved into files of its own
		*1.2.1* A renumbered Extension/User takes its call history with it, and its mailbox now keeps the alias that makes message waiting and *97 work
		*1.2.0* The Extension/User number is editable: it renumbers the extension, its User Manager account, its mailbox and any handset pointed at it, and a number already in use is refused
		*1.1.0* Extension/User devices create and remove their user, User Manager account and email; sequential device ids; reload on save and delete
		*1.0.1* New
	</changelog>
	<category>Oryk</category>
	<description>Connect to Devices.</description>
	<more-info>https://oryk.com/voip/devices</more-info>
	<location>System Banner</location>
	<files>
		<file>functions.inc.php</file>
	</files>
	<menuitems>
		<oryk_connect category="Oryk" order="3">
			<![CDATA[
				<i class="fa fa-sitemap"></i>
				<span>Connect</span>
			]]>
		</oryk_connect>
	</menuitems>
	<depends>
		<module>core</module>
		<module>userman</module>
	</depends>
	<supported>
		<version>16.0</version>
		<version>17.0</version>
	</supported>
</module>